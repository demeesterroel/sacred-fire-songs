## 2026-07-14 (Media Player UX & E2E Test Lint Fixes)

### 1. Rehearsal Recording E2E Test Fixes
- Resolved untyped `page: any` parameter warning by importing and using the `Page` type from `@playwright/test` in [recording.spec.ts](file:///home/roeland/projects/sacred-fire-songs/e2e/tests/recording.spec.ts).
- Fixed `Unexpected any` error on `window` object by casting it to `unknown as { __E2E_FAST_TIMER__: boolean }`.
- Replaced `// @ts-ignore` with `// @ts-expect-error - overriding window.Blob for testing` to satisfy ESLint documentation requirements.

### 2. Parent Repository Sync
- Pinned the `engine` submodule in `songbook-rocks` to the latest commit and pushed to `feat/issue-187-media-player-ux` to trigger the Vercel preview deployment.

## 2026-07-16 (Performance Testing SSO Bypass Configuration)

### 1. Vercel SSO Bypass Integration
- Modified all k6 scripts in `testing/performance/tests/scripts/` to include the `x-vercel-protection-bypass` header.
- Cleaned the codebase to read the bypass token dynamically from `__ENV.VERCEL_PROTECTION_BYPASS_TOKEN`, ensuring no cleartext secrets are checked into the repository.
- Fixed trailing double-slash requests in `load-test-auth.js` that caused WAF blocks.

### 2. Execution & Reporting
- Executed the performance load tests against the new Vercel Preview URL `https://songbook-rocks-lfcaj6onn-roeland-de-meesters-projects.vercel.app/` using the regenerated bypass token.
- Achieved a **100% success rate** (0% errors across all 80 requests) with an average response time of `501ms`.
- Updated `PERFORMANCE_TEST_REPORT.md` and `RESULTS.md` with the new successful performance metrics.
